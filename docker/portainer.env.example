# =====================================================
# BBT CONNECT - Variáveis de Ambiente (Portainer)
# Copie para: Environment Variables no Stack
# =====================================================

# ======== SEGURANÇA (OBRIGATÓRIO MUDAR) ========
JWT_SECRET=sua_chave_jwt_super_secreta_512bits_mudar_em_producao
POSTGRES_PASSWORD=BbtConnect2026Prod!
MSSQL_PASSWORD=Cdq13xJqsl2t21DTUbbqol

# ======== PGADMIN (Opcional) ========
PGADMIN_EMAIL=admin@bbttransportes.com.br
PGADMIN_PASSWORD=senhaForte123!

# ======== ERP SSW (SQL Server) ========
# Já configurado no stack, mas pode override aqui:
# MSSQL_HOST=177.136.206.200
# MSSQL_PORT=1433
# MSSQL_DATABASE=DBExpress
# MSSQL_USER=mcp_readonly

# ======== DOMÍNIOS (Traefik) ========
# Configurar no DNS apontando para o servidor:
# api.bbttransportes.com.br -> IP servidor
# connect.bbttransportes.com.br -> IP servidor
